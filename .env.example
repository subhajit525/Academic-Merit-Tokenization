# Academic Merit Tokenization — Environment Variables
#
# Copy this file to `.env` and fill in your values.
# The VITE_ prefix is required for Vite to expose them to the frontend.

# Soroban Smart Contract ID (deployed on Stellar Testnet)
# Deploy your contract with: stellar contract deploy --wasm <path> --network testnet --source <key>
VITE_CONTRACT_ID=CDLZFC3SYJYDZT7K67VZ75HPJVIEUVNIXF47ZG2FB2RMQQVU2HHGCYSC
